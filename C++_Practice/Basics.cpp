#include <iostream>  // Make sure this line is present

using namespace std; // This allows using cout without std::

int main() {
    cout << "Hello, World!" << endl; // Should work now
    return 0;
}
